<UserControl x:Class="GoatClient.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:GoatClient.Controls"
             xmlns:vm="clr-namespace:GoatClient.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProfilesViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel>
            <Button DockPanel.Dock="Right" Style="{StaticResource GlowButton}" Command="{Binding CreateProfileCommand}"
                    VerticalAlignment="Center" Padding="22,11" FontSize="13">
                <StackPanel Orientation="Horizontal">
                    <Path Data="{StaticResource IconPlus}" Fill="White" Width="12" Height="12" Stretch="Uniform" Margin="0,0,8,0" />
                    <TextBlock Text="NEW PROFILE" />
                </StackPanel>
            </Button>
            <StackPanel>
                <TextBlock Text="PROFILES" Style="{StaticResource PageTitle}" />
                <TextBlock Style="{StaticResource PageSubtitle}"><Run Text="{Binding CountText, Mode=OneWay}" /> · stored in %APPDATA%\GoatClient\profiles.json</TextBlock>
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" Margin="0,22,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Profiles}" Focusable="False">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:ProfileCard Width="330" Margin="0,0,16,16" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Editor overlay -->
        <Grid Grid.RowSpan="2" Background="{StaticResource OverlayBrush}" Margin="-32,-6,-32,-26"
              Visibility="{Binding IsEditorOpen, Converter={StaticResource BoolToVisibility}}">
            <Border Style="{StaticResource ModernCard}" Width="580" Padding="28" HorizontalAlignment="Center" VerticalAlignment="Center"
                    BorderBrush="{StaticResource BorderStrongBrush}" DataContext="{Binding Editor}" MaxHeight="660">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="{Binding Title}" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" />

                    <StackPanel DockPanel.Dock="Bottom">
                        <Border CornerRadius="10" Background="{StaticResource DangerSoftBrush}" Padding="12,9" Margin="0,14,0,0"
                                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}">
                            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" FontSize="12" TextWrapping="Wrap" />
                        </Border>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                            <Button Style="{StaticResource ModernButton}" Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,10,0" IsCancel="True" />
                            <Button Style="{StaticResource GlowButton}" Content="{Binding SaveText}" Command="{Binding SaveCommand}" Padding="24,10" FontSize="13" />
                        </StackPanel>
                    </StackPanel>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,0,6,0">
                            <TextBlock Text="Name" Style="{StaticResource FieldLabel}" />
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="32" />

                            <Grid Margin="0,16,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="14" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Minecraft version" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding Versions}" SelectedValuePath="Id" SelectedValue="{Binding MinecraftVersion}" DisplayMemberPath="Id" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="RAM" Style="{StaticResource FieldLabel}" />
                                    <ComboBox ItemsSource="{Binding RamOptions}" SelectedValuePath="Mb" SelectedValue="{Binding RamMb}"
                                              DisplayMemberPath="Label" ItemContainerStyle="{StaticResource RamComboItem}" />
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Java runtime" Style="{StaticResource FieldLabel}" Margin="0,16,0,7" />
                            <ComboBox ItemsSource="{Binding JavaPreferences}" SelectedValuePath="Value" SelectedValue="{Binding JavaPreference}" DisplayMemberPath="Label" />
                            <TextBlock Text="{Binding JavaHint}" Style="{StaticResource MutedText}" FontSize="11" Margin="2,6,0,0" />

                            <TextBlock Text="Game directory" Style="{StaticResource FieldLabel}" Margin="0,16,0,7" />
                            <DockPanel>
                                <Button DockPanel.Dock="Right" Style="{StaticResource ModernButton}" Content="Browse…" Command="{Binding BrowseGameDirectoryCommand}" Margin="8,0,0,0" />
                                <TextBox Text="{Binding GameDirectory, UpdateSourceTrigger=PropertyChanged}" />
                            </DockPanel>

                            <TextBlock Text="JVM arguments (optional)" Style="{StaticResource FieldLabel}" Margin="0,16,0,7" />
                            <TextBox Text="{Binding JvmArguments, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas" />
                            <TextBlock Text="Memory is controlled by the RAM setting – do not add -Xmx / -Xms here." Style="{StaticResource MutedText}" FontSize="11" Margin="2,6,0,0" />
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
