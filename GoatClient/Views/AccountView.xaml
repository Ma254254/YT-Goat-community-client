<UserControl x:Class="GoatClient.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:GoatClient.Controls"
             xmlns:vm="clr-namespace:GoatClient.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AccountViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="ACCOUNT" Style="{StaticResource PageTitle}" />
            <TextBlock Text="Your Minecraft account connection." Style="{StaticResource PageSubtitle}" />
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Margin="0,22,0,0" Padding="48">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="520">
                <Border Width="84" Height="84" CornerRadius="42" Background="{StaticResource SecondaryBrush}" HorizontalAlignment="Center">
                    <Path Data="{StaticResource IconAccount}" Fill="{StaticResource MutedBrush}" Width="36" Height="36" Stretch="Uniform" />
                </Border>
                <TextBlock Text="{Binding StatusText}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,22,0,0" />
                <TextBlock Text="{Binding Title}" Style="{StaticResource BodyText}" FontSize="15" HorizontalAlignment="Center" Margin="0,8,0,0" />
                <Border Style="{StaticResource Badge}" HorizontalAlignment="Center" Margin="0,10,0,0" Padding="12,5">
                    <TextBlock Text="{Binding Phase}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
                </Border>
                <TextBlock Style="{StaticResource MutedText}" TextAlignment="Center" Margin="0,18,0,24" FontSize="14"
                           Text="GOAT CLIENT currently stores no account data, usernames or tokens. Secure sign-in with your Microsoft account will be added in Phase 2." />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Path Data="{StaticResource IconLock}" Fill="{StaticResource MutedBrush}" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0" />
                    <TextBlock Text="Passwords are never entered in the launcher." Style="{StaticResource MutedText}" FontSize="12" />
                </StackPanel>
                <Button Style="{StaticResource ModernButton}" Content="LOGIN WITH MICROSOFT" Command="{Binding LoginCommand}" HorizontalAlignment="Center" Margin="0,24,0,0" Padding="26,12" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
