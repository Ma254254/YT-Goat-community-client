<UserControl x:Class="GoatClient.Controls.TransferPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:GoatClient.Controls"
             xmlns:vm="clr-namespace:GoatClient.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:GameController}">
    <!-- DataContext: GameController. Visible only while an install/repair/launch is running. -->
    <Border CornerRadius="12" Background="{StaticResource InputBrush}" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" Padding="16,14"
            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" FontSize="12" Foreground="{StaticResource MutedBrush}"
                               Text="{Binding Progress, StringFormat={}{0:0}%}"
                               Visibility="{Binding IsIndeterminate, Converter={StaticResource InverseBoolToVisibility}}" />
                    <TextBlock Text="{Binding StageText}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis" />
                </DockPanel>
                <ProgressBar Style="{StaticResource TransferProgressBar}" Margin="0,10,0,8" Value="{Binding Progress, Mode=OneWay}" IsIndeterminate="{Binding IsIndeterminate}" />
                <DockPanel>
                    <TextBlock DockPanel.Dock="Right" Text="{Binding SpeedText}" FontSize="11" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="{Binding DetailText}" FontSize="11" Foreground="{StaticResource MutedBrush}" />
                </DockPanel>
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource ModernButton}" Content="Cancel" Command="{Binding CancelCommand}" Margin="16,0,0,0" VerticalAlignment="Center" />
        </Grid>
    </Border>
</UserControl>
